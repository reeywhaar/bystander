dist
node_modules
package-lock.json

# Generated by scripts/fetch-fonts.sh. Formatting it would leave the committed file
# differing from what the script writes, so a rerun would show up as a diff that means
# nothing.
src/fonts/fonts.css
